# Help
tshark -h

# List available capture interfaces
tshark -D

# Capture traffic from interface
tshark -i <interface>

# Additionnal options:
#  Capture filter (fetch) = -f
#  Display filter (Yank)  = -Y

#  capture to file
         -w <filename>.pcap
#  capture for time duration
         -a duration:<time>
#  capture number of packets
         -c <number>
#  capture specific src/dst address
         -f "host <IP>"
#  capture specific protocol (tcp, udp, dns ...)
         -f "<protocol>"
#  capture from multiple ports
         -Y "udp.port == 320 || udp.port == 319"

# Read a Pcap capture file
tshark -r <pcap-file>.pcap

